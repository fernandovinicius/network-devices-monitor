## Configurações de Ambiente

Criar um arquivo .env com as variáveis:

```.env
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

DB_HOST=
DB_PORT=
DB_USER=
DB_PASS=
DB_NAME=
```

## Execução Banco de Dados
``` docker
docker-compose up -d
```